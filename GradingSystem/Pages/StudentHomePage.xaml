<Page x:Class="GradingSystem.Pages.StudentHomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1200"
      Background="#F5F7FA">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/cardStyles.xaml"/>
                <ResourceDictionary Source="/Styles/buttonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Card Shadow Effect -->
            <DropShadowEffect x:Key="CardShadow" Color="#000000" Opacity="0.08" BlurRadius="15" ShadowDepth="2"/>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Style="{StaticResource PageHeader}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="WelcomeText" Text="Добро пожаловать!" Style="{StaticResource PageTitle}"/>
                    <TextBlock x:Name="DateText" Text="Сегодня" Style="{StaticResource PageSubtitle}"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="RefreshButton" Style="{StaticResource RefreshButton}" Click="RefreshButton_Click" ToolTip="Обновить данные">
                    <Image Source="/Icons/refresh.svg" Width="18" Height="18"/>
                </Button>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="25,20,25,20">

                <!-- Карточки статистики -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Карточка: Курсы -->
                    <Border Grid.Column="0" Style="{StaticResource Card}" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Мои курсы" FontSize="12" Foreground="#7D7D88" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TotalCoursesText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#2196F3" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="45" Height="45" Background="#E3F2FD" CornerRadius="22" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Image Source="/Icons/book-open.svg" Width="22" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Карточка: К выполнению -->
                    <Border Grid.Column="2" Style="{StaticResource Card}" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="К выполнению" FontSize="12" Foreground="#7D7D88" FontWeight="SemiBold"/>
                                <TextBlock x:Name="PendingAssignmentsText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#FF9800" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="45" Height="45" Background="#FFF3E0" CornerRadius="22" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Image Source="/Icons/file-text.svg" Width="22" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Карточка: Выполнено -->
                    <Border Grid.Column="4" Style="{StaticResource Card}" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Выполнено" FontSize="12" Foreground="#7D7D88" FontWeight="SemiBold"/>
                                <TextBlock x:Name="CompletedAssignmentsText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#4CAF50" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="45" Height="45" Background="#E8F5E9" CornerRadius="22" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Image Source="/Icons/check-circle.svg" Width="22" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Карточка: Средний балл -->
                    <Border Grid.Column="6" Style="{StaticResource Card}" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Средний балл" FontSize="12" Foreground="#7D7D88" FontWeight="SemiBold"/>
                                <TextBlock x:Name="AverageGradeText" Text="0.0" FontSize="32" FontWeight="Bold" Foreground="#9C27B0" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="45" Height="45" Background="#F3E5F5" CornerRadius="22" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Image Source="/Icons/star.svg" Width="22" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Основной контент -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Ближайшие дедлайны -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Ближайшие дедлайны" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                            </Grid>
                            <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="DeadlinesList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FAFAFA" CornerRadius="8" Padding="15" Margin="0,0,0,10" BorderBrush="#EEEEEE" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50" TextWrapping="Wrap"/>
                                                        <TextBlock Text="{Binding CourseName}" FontSize="12" Foreground="#666" Margin="0,3,0,0"/>
                                                        <TextBlock Text="{Binding Date}" FontSize="12" Foreground="#FF9800" Margin="0,5,0,0"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                                                        <TextBlock Text="{Binding DaysLeft}" FontSize="24" FontWeight="Bold" Foreground="#FF5722" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="дней" FontSize="11" Foreground="#666" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <TextBlock x:Name="NoDeadlinesMessage" Text="Нет предстоящих дедлайнов" FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>

                    <!-- Последняя активность -->
                    <Border Grid.Column="2" Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Последняя активность" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                            </Grid>
                            <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                                <ItemsControl x:Name="RecentActivityList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FAFAFA" CornerRadius="8" Padding="12" Margin="0,0,0,8" BorderBrush="#EEEEEE" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Grid.Column="0" Width="35" Height="35" Background="{Binding IconBackground}" CornerRadius="17" Margin="0,0,10,0">
                                                        <TextBlock Text="{Binding Icon}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#666" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="2" Text="{Binding Time}" FontSize="10" Foreground="#999" VerticalAlignment="Top" Margin="10,0,0,0"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <TextBlock x:Name="NoActivityMessage" Text="Нет активности" FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
