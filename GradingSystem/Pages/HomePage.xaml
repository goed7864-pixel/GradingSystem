<Page x:Class="GradingSystem.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="HomePage"
      Background="#F5F5F5">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/cardStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PageHeader}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="WelcomeText" Text="Добро пожаловать!" Style="{StaticResource PageTitle}"/>
                    <TextBlock x:Name="DateText" Text="Пятница, 23 мая 2026" Style="{StaticResource PageSubtitle}"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="RefreshButton" Style="{StaticResource RefreshButton}" Click="RefreshButton_Click" ToolTip="Обновить данные">
                    <Image Source="/Icons/refresh.svg" Width="18" Height="18"/>
                </Button>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="25,20,25,20">

                <Grid x:Name="StatisticsGrid" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border x:Name="TotalGroupsCard" Grid.Column="0" Style="{StaticResource Card}" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Всего групп" FontSize="12" Foreground="#7D7D88" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TotalGroupsText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#5ED0BA" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="45" Height="45" Background="#E8F5E9" CornerRadius="22" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Image Source="/Icons/users.svg" Width="22" Height="22"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border x:Name="TotalStudentsCard" Grid.Column="2" Style="{StaticResource Card}" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock x:Name="StudentsCardTitle" Text="Студентов" FontSize="12" Foreground="#7D7D88" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TotalStudentsText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#5677D8" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="45" Height="45" Background="#E3F2FD" CornerRadius="22" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Image Source="/Icons/graduation-cap.svg" Width="22" Height="22"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border x:Name="TotalAssignmentsCard" Grid.Column="4" Style="{StaticResource Card}" Padding="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock x:Name="AssignmentsCardTitle" Text="Заданий" FontSize="12" Foreground="#7D7D88" FontWeight="SemiBold"/>
                                <TextBlock x:Name="TotalAssignmentsText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#FF9800" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="45" Height="45" Background="#FFF3E0" CornerRadius="22" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Image Source="/Icons/file-text.svg" Width="22" Height="22"/>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <Border Style="{StaticResource Card}" Margin="0,0,0,20">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="Последняя активность" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                                    <TextBlock Text="Сегодня" FontSize="11" Foreground="Gray" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                                <ScrollViewer MaxHeight="280" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="RecentActivityList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#FAFAFA" CornerRadius="8" Padding="12" Margin="0,0,0,8" BorderBrush="#EEEEEE" BorderThickness="1">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Border Grid.Column="0" Width="35" Height="35" Background="{Binding IconBackground}" CornerRadius="17" Margin="0,0,10,0">
                                                            <TextBlock Text="{Binding Icon}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#666" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="2" Text="{Binding Time}" FontSize="10" Foreground="#999" VerticalAlignment="Top" Margin="10,0,0,0"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                                <TextBlock x:Name="NoActivityMessage" Text="Нет активности" FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Border x:Name="MyGroupsPanel" Style="{StaticResource Card}" Margin="0,0,0,20">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="Мои группы" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                                    <Button x:Name="AddGroupButton" Width="30" Height="30" Background="#4CAF50" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" Click="AddGroupButton_Click" ToolTip="Добавить группу">
                                        <TextBlock Text="+" FontSize="18" FontWeight="Bold" Foreground="White"/>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#45a049"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>

                                <Border Style="{StaticResource SearchBoxBorder}" Margin="0,0,0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Image Source="/icons/search.svg" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBox x:Name="SearchBox" Grid.Column="1" Style="{StaticResource SearchTextBox}" TextChanged="SearchBox_TextChanged">
                                        </TextBox>
                                    </Grid>
                                </Border>

                                <ScrollViewer MaxHeight="320" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="GroupsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#FAFAFA" CornerRadius="8" Padding="10" Margin="0,0,0,6" BorderBrush="#EEEEEE" BorderThickness="1" Cursor="Hand">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#F0F0F0"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                                            <TextBlock Text="{Binding StudentCount, StringFormat='👤 {0} студентов'}" FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                                                        </StackPanel>
                                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                                            <Button Width="26" Height="26" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,2,0" Click="EditGroupButton_Click" Tag="{Binding Id}" ToolTip="Редактировать">
                                                         
                                                                <Button.Style>
                                                                    <Style TargetType="Button">
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Button">
                                                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="4">
                                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                    </Border>
                                                                                    <ControlTemplate.Triggers>
                                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                                            <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                                                                        </Trigger>
                                                                                    </ControlTemplate.Triggers>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                    </Style>
                                                                </Button.Style>
                                                            </Button>
                                                            <Button Width="26" Height="26" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="DeleteGroupButton_Click" Tag="{Binding Id}" ToolTip="Удалить">
                                                                <Image Source="/Icons/trash.svg" Width="12" Height="12"/>
                                                                <Button.Style>
                                                                    <Style TargetType="Button">
                                                                        <Setter Property="Template">
                                                                            <Setter.Value>
                                                                                <ControlTemplate TargetType="Button">
                                                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="4">
                                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                    </Border>
                                                                                    <ControlTemplate.Triggers>
                                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                                            <Setter TargetName="border" Property="Background" Value="#FFCDD2"/>
                                                                                        </Trigger>
                                                                                    </ControlTemplate.Triggers>
                                                                                </ControlTemplate>
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                    </Style>
                                                                </Button.Style>
                                                            </Button>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <TextBlock x:Name="NoGroupsMessage" Text="Группы не найдены" FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Ближайшие дедлайны" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,12"/>
                                <ItemsControl x:Name="DeadlinesList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#FFF9C4" CornerRadius="8" Padding="10" Margin="0,0,0,6" BorderBrush="#FBC02D" BorderThickness="1">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                                    <TextBlock Text="{Binding Date}" FontSize="10" Foreground="#F57C00" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock x:Name="NoDeadlinesMessage" Text="Нет ближайших дедлайнов" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" Margin="0,12,0,0" Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>