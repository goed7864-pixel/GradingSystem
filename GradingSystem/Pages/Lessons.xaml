<Page x:Class="GradingSystem.Pages.LessonsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="LessonsPage"
      Background="#F5F5F5">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/cardStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PageHeader}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Управление курсами" Style="{StaticResource PageTitle}"/>
                    <TextBlock Text="Создание и управление заданиями" Style="{StaticResource PageSubtitle}"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="RefreshButton" Style="{StaticResource RefreshButton}" Click="RefreshButton_Click" ToolTip="Обновить данные">
                    <Image Source="/Icons/refresh.svg" Width="18" Height="18"/>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая панель - Список курсов -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0">
                <StackPanel>
                    <Border Style="{StaticResource SectionHeader}">
                        <StackPanel>
                            <TextBlock Text="Мои курсы" Style="{StaticResource SectionTitle}" Margin="0,8,0,0"/>
                            <TextBlock x:Name="CoursesCountText" Text="0 курсов" FontSize="12" Foreground="White" Opacity="0.8" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SearchBoxBorder}" Margin="15,15,15,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="/Icons/search.svg" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox x:Name="SearchBox" Grid.Column="1" Style="{StaticResource SearchTextBox}" TextChanged="SearchBox_TextChanged">
                            </TextBox>
                        </Grid>
                    </Border>

                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="450" Margin="0,0,0,15">
                        <ItemsControl x:Name="CoursesList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="CourseItem" Background="Transparent" Padding="15,12" Margin="0,0,0,2" Cursor="Hand" MouseLeftButtonDown="CourseItem_Click" Tag="{Binding Id}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F0F0F0"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="40" Height="40" Background="{Binding Color}" CornerRadius="10" Margin="0,0,12,0">
                                                <TextBlock Text="{Binding Icon}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Width="26" Height="26" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,2,0" Click="EditCourseButton_Click" Tag="{Binding Id}" ToolTip="Редактировать курс">
                                                    <Image Source="/Icons/edit.svg" Width="12" Height="12"/>
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="4">
                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                                <Button Width="26" Height="26" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="DeleteCourseButton_Click" Tag="{Binding Id}" ToolTip="Удалить курс">
                                                    <Image Source="/Icons/trash.svg" Width="12" Height="12"/>
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="4">
                                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter TargetName="border" Property="Background" Value="#FFCDD2"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Height="1" Background="#E0E0E0" Margin="15,0"/>

                    <Button x:Name="AddCourseButton" Style="{StaticResource AddButton}" Click="AddCourseButton_Click" Margin="15,15,15,20">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Image Source="/Icons/plus.svg" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Создать новый курс" FontSize="13" FontWeight="SemiBold" Foreground="#5ED0BA"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Правая панель - Задания выбранного курса -->
            <Border Grid.Column="2" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#FAFAFA" CornerRadius="15,15,0,0" Padding="20" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock x:Name="SelectedCourseName" Text="Выберите курс" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                                <TextBlock x:Name="SelectedCourseInfo" Text="Нажмите на курс слева" FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
                            </StackPanel>
                            <Button Grid.Column="1" x:Name="AddAssignmentButton" Width="100" Height="36" Background="#4CAF50" BorderThickness="0" Cursor="Hand" Click="AddAssignmentButton_Click" ToolTip="Добавить задание" Visibility="Collapsed">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="+" FontSize="16" Margin="0,0,5,0"/>
                                    <TextBlock Text="Задание" FontSize="13" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#45a049"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1" Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#F5F5F5" Padding="12" Margin="0,0,0,15" CornerRadius="10" Visibility="Collapsed" x:Name="AssignmentSearchPanel">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Source="/Icons/search.svg" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox x:Name="AssignmentSearchBox" Grid.Column="1" Background="Transparent" BorderThickness="0" FontSize="13" VerticalAlignment="Center" TextChanged="AssignmentSearchBox_TextChanged">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="Foreground" Value="#333"/>
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                                <VisualBrush.Visual>
                                                                    <TextBlock Text="Поиск заданий..." Foreground="#999" FontSize="13"/>
                                                                </VisualBrush.Visual>
                                                            </VisualBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            </Grid>
                        </Border>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="AssignmentsList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#FAFAFA" CornerRadius="10" Padding="15" Margin="0,0,0,10" BorderBrush="#EEEEEE" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" Width="45" Height="45" Background="{Binding Color}" CornerRadius="10" Margin="0,0,12,0">
                                                    <TextBlock Text="{Binding Icon}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>

                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                                    <TextBlock Text="{Binding Description}" FontSize="11" Foreground="Gray" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                        <Border Background="#FFF3E0" CornerRadius="8" Padding="6,2">
                                                            <TextBlock Text="{Binding Deadline}" FontSize="10" Foreground="#FF9800"/>
                                                        </Border>
                                                        <Border Background="#E3F2FD" CornerRadius="8" Padding="6,2" Margin="8,0,0,0">
                                                            <TextBlock Text="{Binding SubmissionsCount, StringFormat='📋 {0} сдано'}" FontSize="10" Foreground="#2196F3"/>
                                                        </Border>
                                                    </StackPanel>
                                                </StackPanel>

                                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Button Width="32" Height="32" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="EditAssignmentButton_Click" Tag="{Binding Id}" ToolTip="Редактировать">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="6">
                                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            </Border>
                                                                            <ControlTemplate.Triggers>
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                                                                </Trigger>
                                                                            </ControlTemplate.Triggers>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Style>
                                                        </Button.Style>
                                                        <TextBlock Text="✏️" FontSize="14"/>
                                                    </Button>
                                                    <Button Width="32" Height="32" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="5,0,0,0" Click="DeleteAssignmentButton_Click" Tag="{Binding Id}" ToolTip="Удалить">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="6">
                                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            </Border>
                                                                            <ControlTemplate.Triggers>
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter TargetName="border" Property="Background" Value="#FFCDD2"/>
                                                                                </Trigger>
                                                                            </ControlTemplate.Triggers>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Style>
                                                        </Button.Style>
                                                        <Image Source="/Icons/trash.svg" Width="14" Height="14"/>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="30" x:Name="NoAssignmentsPanel" Visibility="Collapsed">
                            <StackPanel HorizontalAlignment="Center">
                                <Image Source="/Icons/file-text.svg" Width="48" Height="48" HorizontalAlignment="Center"/>
                                <TextBlock Text="Нет заданий" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,15,0,5"/>
                                <TextBlock Text="В этом курсе пока нет заданий" FontSize="13" Foreground="Gray" HorizontalAlignment="Center"/>
                                <Button x:Name="EmptyStateAddButton" Height="40" Background="#4CAF50" BorderThickness="0" Cursor="Hand" Click="AddAssignmentButton_Click" Margin="0,20,0,0" Width="200">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#45a049"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="+" FontSize="16" Margin="0,0,8,0"/>
                                        <TextBlock Text="Создать задание" FontSize="13" Foreground="White" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>